# Server-only keys (never prefix with VITE_).
# Used by /api/* token + persistence endpoints.

DEEPGRAM_API_KEY=your_deepgram_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# Neon Postgres. Required to persist runs, accounts, and the live leaderboard.
DATABASE_URL=postgres://user:pass@host/neondb

# Comma-separated providers for a round. Default is all three.
# The live caret follows the current top model, not a fixed vendor.
# VITE_BENCH_PROVIDERS=deepgram,openai,elevenlabs
